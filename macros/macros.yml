version: 2

macros:
  - name: add_etl_timestamp
    description: |
      This macro generates a timestamp representing the current date and time when executed.
      It is useful for tracking ETL processes by adding a standardized `etl_timestamp` column
      in transformation queries.
    arguments: []
